DATA SEGMENT
MSG1 DB "IT IS ODD$"
MSG2 DB "IT IS EVEN$"
DATA ENDS

PRINT MACRO MSG
MOV AH, 09H
LEA DX, MSG
INT 21H
ENDM

CODE SEGMENT
ASSUME CS:CODE, DS:DATA

START:  MOV AX,DATA
        MOV DS, AX
        MOV AX, 0000H
        MOV AX, 8D
        SHR AX,01H
        JNC SUCH
        PRINT MSG2
        JMP FINISH

SUCH:   PRINT MSG2
        JMP FINISH

FINISH: HLT
        CODE ENDS
        END START
